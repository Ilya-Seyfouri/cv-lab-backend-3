[phases.setup]
aptPkgs = ["texlive-latex-base", "texlive-fonts-recommended", "texlive-latex-extra", "texlive-fonts-extra"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port $PORT"